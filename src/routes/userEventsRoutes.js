const express = require("express");

const router = express.Router();

// Remove as rotas relacionadas a eventos
// router.get("/user-events", async (req, res) => { ... });
// router.get("/company-events", async (req, res) => { ... });

module.exports = router;
